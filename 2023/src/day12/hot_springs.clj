(ns day12.hot-springs)

(def input (slurp "src/day12/input"))
(def example-good (slurp "src/day12/example.good"))
(def example-bad (slurp "src/day12/example.bad"))

(defn parse [in]
  in)

(defn part1 [in]
  in)

(assert (= (part1 (parse example-good))))
(assert (= (part1 (parse input))))

(defn part2 [in]
  in)

(assert (= (part2 (parse example-good))))
(assert (= (part2 (parse input))))
