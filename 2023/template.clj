(ns day.template)

(def input (slurp "src/day/input"))
(def example (slurp "src/day/example"))

(defn parse [in]
  in)

(defn part1 [in]
  in)

(assert (= (part1 (parse example))))
(assert (= (part1 (parse input))))

(defn part2 [in]
  in)

(assert (= (part2 (parse example))))
(assert (= (part2 (parse input))))
